# Postgres connection (Railway sets DATABASE_URL automatically when you add a PostgreSQL plugin)
DATABASE_URL=postgres://user:pass@host:5432/dbname
# Exact frontend origin(s) allowed to send credentialed requests (comma-separated). No trailing slash.
CORS_ORIGIN=https://loothood.vercel.app
# Port (Railway injects PORT automatically)
PORT=3000
